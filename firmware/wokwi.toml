# Wokwi Project Configuration for GPS Dog Collar

[wokwi]
version = 1
firmware = "collar-firmware.elf"
board = "esp32dev"

# ESP32 DevKit V1
[board]
mcu = "esp32"
flash_size = "4MB"

# Simulation timeout (0 = unlimited)
timeout = 0

# Virtual serial monitor
[serial]
enabled = true
